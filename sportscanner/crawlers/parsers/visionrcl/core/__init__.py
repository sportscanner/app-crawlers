### Vision RCL backend provider is a flow.onl (Gladstone) deployment, same as
### Active Lambeth. The Better strategy is reused; only the request builder
### differs (v2-only activity slugs derived from category slugs).
